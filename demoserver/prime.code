// c0-c4 — candidates in the list, one of which not prime
// p, q — non-trivial factors for the non-prime candidate
def main(field c0, field c1, field c2, field c4) -> (field): 
	field res = 0
  	res = if c2>c0 && c2<c1 then 1*c4*c2 else 0 fi
	return res
